[package]
name = "h12tiny-client-normalize"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Direct-origin HTTP request normalization shared by h12tiny clients"
license = "MIT OR Apache-2.0"

[dependencies]
http = { workspace = true }
